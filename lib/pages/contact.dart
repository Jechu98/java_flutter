import 'package:flutter/material.dart';

//TabBar
/*
class Contact extends StatelessWidget{
  @override
  Widget build(BuildContext context){
    return new Container(
      child: new Column(
        children: <Widget> [
          new Icon(Icons.add, size: 35, color: Colors.amberAccent,),
          new Text("Tercera Tab")
        ],
      ),
    );
  }
}
 */
//Bottom Navigation Bar
/*
class tres extends StatelessWidget{
  @override
  Widget build(BuildContext context){
    return new Scaffold(
      backgroundColor: Colors.black,
      body: new Container(
        child: new Center(
          child: new Column(
            mainAxisAlignment: MainAxisAlignment.center,
            children: <Widget> [
              new Icon(Icons.add, size: 160.0, color: Colors.grey,),
              new Text("Third", style: new TextStyle(color: Colors.white),)
            ],
          ),
        ),
      ),
    );
  }
}
 */

//home Drawer
/*
class Contact extends StatelessWidget{
  static const String routeName = "/contact";
  @override
  Widget build(BuildContext context){
    return new Scaffold(
      appBar: new AppBar(
        title: new Text("Contact"),
      ),
      body: new Container(
        child: new Center(
          child: new Text("Pantalla de Contact"),
        ),
      ),
    );
  }
}
 */
